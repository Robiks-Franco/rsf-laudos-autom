# Dependências do APLICATIVO WEB (app_web.py), publicado em um serviço
# de hospedagem como o Render.com. Inclui as mesmas dependências do
# aplicativo de mesa (requirements.txt) mais o servidor web.
#
# Repare que o Tkinter NÃO aparece aqui — o app web não usa janela
# gráfica, então não precisa dele.

anthropic>=0.40.0
python-docx>=1.1.0
PyMuPDF>=1.24.0

fastapi>=0.110.0
uvicorn[standard]>=0.29.0
python-multipart>=0.0.9
